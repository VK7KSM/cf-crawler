{
  "name": "cf-crawler-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --out-dir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}

